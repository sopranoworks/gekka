version: "2"

linters:
  disable:
    - errcheck
  settings:
    staticcheck:
      # Enable only correctness checks (SA*); skip style/quickfix rules (QF*, ST*, S1*)
      checks:
        - "-all"
        - "SA*"

output:
  formats:
    text:
      path: stdout
      print-linter-name: true
