// akka-2614-server sub-project — Akka 2.6.14 strict-HOCON JOIN reproducer.
//
// Pinned to Akka 2.6.14 so the reproducer can demonstrate version-specific
// behavior of the JoinConfigCompatChecker that surfaces in the dashboard's
// real production cluster.  This file is intentionally minimal — the parent
// scala-server/build.sbt does the project wiring.
