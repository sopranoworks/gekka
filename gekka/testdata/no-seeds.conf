# Config with canonical address but no seed-nodes (uses reference.conf fallback).
pekko {
  actor.provider = cluster
  remote.artery {
    canonical {
      hostname = "192.168.1.5"
      port = 9000
    }
  }
}
